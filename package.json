{
  "name": "lien-api-landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd dashboard && npm ci && npm run build",
    "postinstall": "cd dashboard && (npm ci || npm install)"
  }
}

